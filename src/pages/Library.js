import React from 'react';
import BooksList from "../components/BooksList";

const Library = () => {
    return (
        <div>
            <BooksList/>
        </div>
    );
};

export default Library;